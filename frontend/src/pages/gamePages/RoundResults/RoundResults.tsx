import React from "react";

import { RoundResultsData } from "../../../interfaces/interfaces";

const RoundResults: React.FC<RoundResultsData> = () => {
    return (
        <div>
            <h1>Round Results</h1>
        </div>
    );
};

// export Map (just scrollable map) to other component?

export default RoundResults;
